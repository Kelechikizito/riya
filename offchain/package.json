{
  "name": "riya-offchain",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "riya's off-chain programs: the Ethereum keeper and the Ethereum-to-Creditcoin readability worker.",
  "scripts": {
    "abi": "tsx scripts/gen-abi.ts",
    "worker": "tsx src/worker.ts",
    "keeper": "tsx src/keeper.ts",
    "typecheck": "tsc --noEmit",
    "test": "node --import tsx --test test/*.test.ts"
  },
  "dependencies": {
    "@gluwa/usc-sdk": "^0.18.0",
    "dotenv": "^17.4.2",
    "ethers": "^6.13.0",
    "exponential-backoff": "^3.1.1"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
