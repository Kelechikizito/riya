# Copy to `.env` and fill in. `.env` is gitignored; keep it that way.
#
# The deploy targets in the Makefile print addresses. Paste them back in here as you go,
# then `make frontend-env` writes frontend/.env.local from these same values.

# ---------------------------------------------------------------------------
# Endpoints
# ---------------------------------------------------------------------------
ETH_MAINNET_RPC_URL=
ETH_SEPOLIA_RPC_URL=
# A second Sepolia endpoint for the worker. Optional, but a node that quietly stops
# delivering logs looks like a quiet day rather than an error.
ETH_SEPOLIA_RPC_URL_FALLBACK=
CREDITCOIN_RPC_URL=https://rpc.cc3-testnet.creditcoin.network
PROVER_URL=https://prover.cc3-testnet.creditcoin.network

SOURCE_CHAIN_ID=11155111
# Sepolia's key in Creditcoin Testnet's registry. Verified against the live precompile
# by the fork tests, and asserted again by the deploy script before it spends gas.
CHAIN_KEY=1

# ---------------------------------------------------------------------------
# Signing
# ---------------------------------------------------------------------------
# No private keys here. All three live in Foundry's encrypted keystore:
#
#   sepolia-acc         deploys and runs the interaction scripts
#   riya-keeper         holds Sepolia ETH, calls harvest()
#   readability-worker  holds tCTC, submits proofs
#
# Import one with: cast wallet import <name> --interactive
#
# Only the deployer's address is needed in plain text. `--account` unlocks the keystore
# too late for simulation, and the deploy scripts predict nonces against this address.
# Get all three with `make senders`.
DEPLOYER_ADDRESS=
# Not used for signing. `make preflight` checks these are funded: the keeper spends Sepolia
# ETH on harvests, the worker spends tCTC on proofs, and neither can be topped up mid-demo.
KEEPER_ADDRESS=
WORKER_ADDRESS=

# Source-chain verification. One key covers both Ethereum networks on Etherscan v2.
# Leave blank to deploy unverified; the Makefile drops --verify when it is unset.
ETHERSCAN_API_KEY=

# ---------------------------------------------------------------------------
# Sepolia mocks — from `make deploy-mocks`
# Aave V4 is live on Ethereum Mainnet and nowhere else, so the demo brings its own.
# ---------------------------------------------------------------------------
MOCK_USD=
MOCK_SPOKE=
MOCK_RESERVE_ID=

# ---------------------------------------------------------------------------
# Ethereum leg — from `make deploy-source`
# ---------------------------------------------------------------------------
RIYA_ESCROW_ADDRESS=
AAVE_V4_ADAPTER_ADDRESS=

# ---------------------------------------------------------------------------
# Creditcoin leg — from `make deploy-destination`
# ---------------------------------------------------------------------------
RIYA_USD_ADDRESS=
RIYA_ASC_ADDRESS=
LOAN_LEDGER_ADDRESS=

# ---------------------------------------------------------------------------
# Frontend
# ---------------------------------------------------------------------------
# The Aave supply APY the dashboard assumes, in basis points. `selfRepayRateBps` takes
# this as an argument rather than reading an oracle, so the UI owns the number.
YIELD_RATE_BPS=500
